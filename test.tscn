[gd_scene load_steps=4 format=2]

[ext_resource path="res://Assets/mission.png" type="Texture" id=1]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform float brightness = 0.4;
uniform float contrast = 1.0;
uniform float saturation = 1.0;

void fragment() {
    vec3 c = textureLod(SCREEN_TEXTURE, SCREEN_UV, 0.0).rgb;

    c.rgb = mix(vec3(0.0), c.rgb, brightness);
    c.rgb = mix(vec3(0.5), c.rgb, contrast);
    c.rgb = mix(vec3(dot(vec3(1.0), c.rgb) * 0.33333), c.rgb, saturation);

    COLOR.rgb = c;
}"
custom_defines = ""

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/brightness = 0.4
shader_param/contrast = 1.0
shader_param/saturation = 1.0

[node name="Node2D" type="Node2D"]
material = SubResource( 2 )

[node name="mission" type="Sprite" parent="."]
position = Vector2( 326.79, 293.27 )
texture = ExtResource( 1 )

[node name="Label" type="Label" parent="."]
margin_left = 193.747
margin_top = 152.735
margin_right = 296.747
margin_bottom = 166.735
rect_scale = Vector2( 3, 3 )
text = "cbefberberbrbre"
__meta__ = {
"_edit_use_anchors_": false
}
